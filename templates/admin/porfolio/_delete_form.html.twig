<form method="post" action="{{ path('app_porfolio_delete', {'id': porfolio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ porfolio.id) }}">
    <button class="btn">Delete</button>
</form>
